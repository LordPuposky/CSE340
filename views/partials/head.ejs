<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="CSE Motors - Vehicle Inventory Management System">
    <title>
        <%= title %> | CSE Motors
    </title>
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/forms.css">
    <link rel="stylesheet" href="/css/inventory.css">

    <!-- Additional CSS for specific pages -->
    <% if (typeof additionalCSS !=='undefined' && additionalCSS) { %>
        <link rel="stylesheet" href="<%= additionalCSS %>">
        <% } %>

            <link rel="icon" type="image/png" href="/images/site/favicon-32x32.png">
            <link href="https://fonts.googleapis.com/css?family=Roboto:400,700&display=swap" rel="stylesheet">
</head>

<body<% if (typeof pageClass !=='undefined' && pageClass) { %> class="<%= pageClass %>"<% } %>>